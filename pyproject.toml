[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pivoterpy"
version = "0.2.0"
description = "Parallelized pure Python implementation of the Pivoter clique counting algorithm."
readme = "README.md"
requires-python = ">=3.7"
dependencies = []

[project.optional-dependencies]
test = [
    "pytest>=7.0",
]
dev = [
    "pytest>=7.0",
]